ignore:
  # We don't care about the coverage of the imported codap files
  - "src/codap"
fixes:
  # In the coverage generated by Playwright when testing the plugin in
  # an iframe the following prefix is added to all of the paths
  - "dst-codap-plugin/::"